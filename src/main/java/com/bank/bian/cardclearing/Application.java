package com.bank.bian.cardclearing;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Card Clearing
 *
 * Business Area:      Operations and Execution
 * Business Domain:    Cards
 * Functional Pattern: Operate
 * Asset Type:         Card Clearing Cycle
 * Control Record:     Card Clearing Cycle Operating Session
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Card Clearing — BIAN Semantic API")
                .description("Service Domain 'Card Clearing' (Operate pattern) "
                        + "operating on the 'Card Clearing Cycle Operating Session' control record.")
                .version("0.1.0"));
    }
}
